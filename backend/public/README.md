# Public Directory

Ce dossier contient les fichiers publics téléchargeables.

## APK Android
Placez votre fichier APK ici avec le nom `app.apk` pour qu'il soit accessible via la route:
```
GET /api/download/apk
```

Le fichier sera téléchargé sous le nom `mirror-area.apk`.
