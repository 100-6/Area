{
  "name": "backend",
  "version": "0.0.1",
  "private": true,
  "description": "Minimal package.json to satisfy CI (no real deps)",
  "scripts": {
    "test": "echo \"No tests defined\" && exit 0",
    "lint": "echo \"No lint configured\" && exit 0"
  }
}
